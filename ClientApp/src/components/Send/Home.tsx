import * as React from 'react';

const Send: React.FC = () => (
  <div>
    <h1>Sending Messages to Configurator</h1> 
    To see how to send messages to configurator, select an activity under the Messages screen.   
  </div>
);

export default Send;
